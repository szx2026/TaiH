@props(['label', 'href' => '#', 'active' => false, 'count' => null, 'tone' => 'slate'])
@php($activeClasses = ['market_research' => 'bg-orange-600', 'website_operations' => 'bg-blue-600', 'content_creative' => 'bg-yellow-500 text-slate-950', 'traffic_growth' => 'bg-violet-600', 'slate' => 'bg-slate-700'])
<a href="{{ $href }}" @class(['mb-1 flex items-center rounded-lg px-3 py-2.5 text-sm transition', ($activeClasses[$tone] ?? $activeClasses['slate']).' font-semibold text-white' => $active, 'text-slate-400 hover:bg-slate-900 hover:text-white' => ! $active])><span class="h-2 w-2 rounded-full {{ $active ? 'bg-white/80' : 'bg-slate-600' }}"></span><span class="ml-3">{{ $label }}</span>@if ($count)<span class="ml-auto rounded bg-slate-800 px-1.5 py-0.5 text-[10px]">{{ $count }}</span>@endif</a>
